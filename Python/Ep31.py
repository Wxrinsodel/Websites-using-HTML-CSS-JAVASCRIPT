#ฝึกการใช้ while 2 โดยกำหนดเงื่อนไข แต่ไม่ได้กำหนดจุดเริ่มกับจุดจบจนกว่าเงื่อนไขจะเป็นเท็จ

sum=0
while sum!=100: #ถ้าผลรวมไม่เท่ากับ 100 ทำต่อได้ จะเปลี่ยนเครื่องหมายก็ได้
    number=int(input("ป้อนตัวเลขของคุณ :"))
    sum+=number
    if sum>100:
        break
    print("ผลรวม =",sum)
